const Vacancy = require('./vacancy.model');
const Category = require('./category.model');
const Application = require('./application.model');

module.exports = Object.create({ Vacancy, Category, Application });
